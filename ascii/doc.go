/*
	The package main/ascii implements functions to transform digits, spaces
	and colons in ASCII art.

	The main function Format, accepts a format string with what it has to
	be the final contents of the returned ASCII art block. It accepts
	any digit secuence, and colons or spaces as separators.
*/
package ascii
